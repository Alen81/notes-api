[pytest]
addopts = --capture=no --verbose --color=yes
markers =
    teardown:marks tests as teardown (deselect with '-m "not teardown"')
    mockdata:marks tests as mockdata (deselect with '-m "not mockdata"')
